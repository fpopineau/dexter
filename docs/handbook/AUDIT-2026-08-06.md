# External audit 2026-08-06 — verified triage and remediation backlog

An external static audit was verified claim-by-claim by three independent
review passes. STATUS: F=fixed same day (commit refs in git log), B=backlog,
D=disputed/overstated. Fix before live: every B marked [LIVE-GATE].

## Criticals
1. F `ibkr_orders place` is now REDUCE-ONLY (close/trim an existing position
   only; refusals route to trade_proposals). The proposal path is the only
   way to add exposure. (2026-08-07)
2. F Approval dialog showed `<unknown>` for trade payloads; session "allow all"
   covered order tools. Fixed: payload shown; trade tools per-order only.
3. F Stale account identity across reconnects. Fixed: cleared on disconnect.
   B: `IBKR_ALLOW_LIVE=true` still waives the empty-list refusal in
   assertAccountsVerified — make identity presence unconditional. [LIVE-GATE]
4. F Auto-exec paper check passed on empty account list. Fixed: fails closed.
5. F Concurrent accepts under-counted (`executing` excluded). Fixed:
   countOpenExecuted/countExecutedSince/symbol exposure all include executing.
6. B Caps count DB positions only, not broker positions (manual TWS trades
   invisible; NetLiq/P&L ARE broker-truthed). Needs broker-snapshot reconcile.
7. B Daily-loss halt-write fail-open (no in-memory latch); baseline recapture
   loop on unwritable data dir. Narrow (needs reqPnL failure too). [LIVE-GATE]
8. B Partial fills: entry recorded only on full fill; partial+cancel closes as
   'cancelled', P&L 0. Protection unaffected (trail/triage read live
   positions); accounting wrong. [LIVE-GATE]
9. B No broker-ack await / no orderRef idempotency on brackets. Mitigations
   exist (executing-sweeper, startup reconcile vs DB ids); reverse sweep for
   unknown broker orders missing. [LIVE-GATE]
10. B closePosition cancels only proposal-tracked exits: protect/auto-protect
    GTC pairs are persisted nowhere and survive a close → reversal risk.
    Fix: cancel by symbol+side like profit-trail does. [LIVE-GATE, HIGH]
11. B Browser follows redirects unvalidated (SSRF to loopback); dashboard CDN
    without SRI/CSP + innerHTML + prefix Origin. web_fetch SSRF claim was
    WRONG (solid validator + manual redirects). Keep dashboard loopback-only.

## Risk-model items
- F Broker-number validation (NaN/sentinel defeated kill-switch and both size
  caps) — fixed in daily-loss-guard (P&L + NetLiq bounds).
- F day-trade skill sized from risk_manager's fake $100K account — now OMIT.
- F Win/loss classified on gross P&L — now net of commissions everywhere.
- B YAML rules unvalidated: NaN-poisoning disables caps; strings pass as
  truthy (earnings_bet_enabled: yes). Schema-check + fail-closed + log.
  [LIVE-GATE, cheap]
- B Earnings-bet evidence advisory (server never checks meetsBar or verifies
  worstCaseGapPct against the record). MUST land before earnings_bet_enabled
  flips on live. [LIVE-GATE]
- B Soft limits: max_sector_exposure_pct / max_overnight_*_pct / min_avg_volume
  enforced nowhere deterministic. Enforce or relabel advisory. [LIVE-GATE]
- B risk_manager duplicates gate/sizer with different math — make it call
  checkProposalRisk/computeQuantity.
- B STP_LMT risk uses trigger not entryLimit (bounded by 0.3-0.6% convention,
  unenforced); MKT entries have no slippage reserve (chase gate bounds it).
- B ATR/EMA context fails open at creation, not refetched at accept.
- B Midday-first-start baseline can hide earlier losses (narrow; per-day file
  + startup capture mitigate).
- D Currency mixing EUR/USD: real but currently CONSERVATIVE (under-sizes).
- B Multi-account ambiguity: orders never set order.account; guard picks
  accounts[0]. Bind one account id everywhere before any multi-account login.

## Infrastructure fixed same day
- F Gateway stop() now stops profit-trail, stale sweeper, EOD triage,
  benchmark, dashboard (were leaked; could place orders post-shutdown).
- F .gitignore malformed joined entry split.
- B agent-runner isRunning lacks try/finally (deterministic trade commands
  unaffected — they route before the check).

## Not independently verified (audit claims, plausible, lower stakes)
- Market-data/opportunity-engine defects (stream lifecycle, listener leaks,
  cache keys, VWAP/RVOL semantics, neutral-score fill-ins).
- Backtester: lookahead, single-bar multi-symbol sim, per-symbol metrics on
  portfolio curve, archive date filtering, sentiment lookahead/substring
  matching. CONSEQUENCE ADOPTED: treat backtest outputs and scorer-weights
  calibration as UNTRUSTED until rebuilt. Do not tune live risk from them.
- Persistence/lifecycle list (migration catches, state-machine looseness,
  in-memory daily caps, cron store rewrite races, debug-log rotation).
- CI/docs nits: pin Bun, add Windows job, CI on day2day branch, release
  script branch push, ARCHITECTURE single-path wording, SEC UA fallback.
